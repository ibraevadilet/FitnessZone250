import 'package:flutter/material.dart';

class RecomendationsScreen extends StatelessWidget {
  const RecomendationsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('RecomendationsScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}